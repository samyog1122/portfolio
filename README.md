# Portfolio

A modern React portfolio built with Vite. This project converts a static landing page into a clean, component-driven React experience with dynamic portfolio filtering, smooth section navigation, and improved responsive styling.

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Start the development server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
```
